package com.example.demo.serviceImpl;

import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.Mockito.when;

import java.util.List;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.MockitoAnnotations;

import com.example.demo.model.Employee;
import com.example.demo.model.EmployeeRequest;
import com.example.demo.model.Role;
import com.example.demo.repository.EmployeeRepository;




class EmployeeServicemplTest {

	@InjectMocks
	EmployeeServiceImpl employeeServiceImpl;
	
	@Mock
	EmployeeRepository employeeRepository;
	
	EmployeeRequest emp = new EmployeeRequest();
	@BeforeEach
	void setUp() throws Exception{
		MockitoAnnotations.initMocks(this);
		}
	
	Role role=new Role();
      @Test
      final void saveEmployeeTest() {
    	  
    	  List<Role> roles;
    	emp.setId(1);
    	emp.setName("Alok");
        emp.setEmployeeId("1559");
        emp.setPassword("Alok@123");
        emp.setRating(4f);
        emp.setSalary(45678l);
        role.setRole("EMPLOYEE");
      
        when(employeeRepository.save(emp)).thenReturn(emp);
        assertEquals(emp, employeeServiceImpl.saveEmployee(employeeRequest));
        
      }
}
